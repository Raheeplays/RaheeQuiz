{
  "rules": {
    // Global lock for unknowns
    ".read": false,
    ".write": false,

    "users": {
      // Allows any authenticated user (including anonymous) to read the user index for login
      ".read": "auth != null",
      "$uid": {
        // Users can only write to their own record matching their Firebase UID
        // We match by the path key ($uid) which should be the auth.uid
        ".write": "auth != null && (
          (!data.exists() && $uid == auth.uid) || 
          ($uid == auth.uid && (
            newData.child('status').val() == data.child('status').val() && 
            newData.child('role').val() == data.child('role').val()
          )) || 
          root.child('users').child(auth.uid).child('role').val() == 'admin'
        )"
      }
    },

    "topics": {
      ".read": "auth != null && root.child('users').child(auth.uid).child('status').val() == 'active'",
      ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() == 'admin'"
    },

    "topicQuizzes": {
      ".read": "auth != null && root.child('users').child(auth.uid).child('status').val() == 'active'",
      ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() == 'admin'"
    },

    "events": {
      ".read": "auth != null && root.child('users').child(auth.uid).child('status').val() == 'active'",
      ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() == 'admin'"
    },

    "history": {
      ".read": "auth != null && root.child('users').child(auth.uid).child('status').val() == 'active'",
      ".write": "auth != null && root.child('users').child(auth.uid).child('status').val() == 'active'"
    },

    "feedback": {
      ".read": "auth != null && root.child('users').child(auth.uid).child('role').val() == 'admin'",
      ".write": "auth != null && root.child('users').child(auth.uid).child('status').val() == 'active'"
    },

    "leaderboard": {
      ".read": "auth != null && root.child('users').child(auth.uid).child('status').val() == 'active'",
      ".write": "auth != null"
    },

    "settings": {
      ".read": "auth != null",
      ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() == 'admin'"
    }
  }
}
